[pytest]
markers =
    integration: tests that hit real paper broker APIs (slow, require credentials)

testpaths = tests
norecursedirs =
    .git
    .venv
    venv
    node_modules
    skool_source_material
