# Alpaca -- PAPER trading keys (never commit the real .env)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
# Live trading requires BOTH AlpacaBroker(paper=False) AND this explicit consent:
# ALPACA_CONFIRM_LIVE=YES
